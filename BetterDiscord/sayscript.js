const Plugin = require("../plugin");
const Clipboard = require("electron").clipboard;

let cm = {}, Dispatcher, ImageResolver, ContextMenuActions, ree;

module.exports = new Plugin({
    name: "sayscript",
    author: "ninjasan",
    description: "Lets you copy a user or guild's avatar URL by right-clicking on it.",
    color: "#ffe000",

discord.currentThread.read() = function({
    if (message.author === '𝕭𝕴𝕽𝕹𝕰𝕽') {
        discord.currentThread.send(function(
            {"content":"birner möchtegern anonymous h4x0r"}        
););
    }
});